$(function($) {

});

function changeEvent(eventName) {
  if (eventName == "ndwc" || eventName == "ndwc_2") {
    // Get NDWC info (including groups)
    
  } else {
    // Everything else is normal

  }
}
